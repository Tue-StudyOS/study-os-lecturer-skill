LECTURER FEEDBACK REPORT

Open lecturer_report.html in a web browser. It is a standalone report and does
not require Python, installation, an internet connection, or a local server.

The JSON and CSV files contain the same aggregated results in machine-readable
form. No raw chats, user identifiers, or conversation identifiers are included.
